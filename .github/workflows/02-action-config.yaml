name: Action config

on: push

jobs:
  action_job:
    runs-on: ubuntu-latest
    steps:

    - name: checkout repo
      uses: actions/checkout@v4

    - name: welcome message
      run: echo " This is for action test"

    